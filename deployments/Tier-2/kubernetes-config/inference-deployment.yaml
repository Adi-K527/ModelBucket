apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-app-deployment
spec:
  replicas: 4
  selector:
    matchLabels:
      app: inference-app
  template:
    metadata:
      labels:
        app: inference-app
    spec:
      containers:
      - image: ${IMAGE_URL}
        name: inference-pod
      imagePullSecrets:
      - name: us-east-1-ecr-registry
